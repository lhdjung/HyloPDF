# Where `cargo run` and `cargo test` find the libpdfium that scripts/pdfium.sh
# (or .ps1) downloaded. Relative to this file's parent, and not forced, so a
# MOONOWL_PDFIUM set in the environment still wins.
[env]
MOONOWL_PDFIUM = { value = "pdfium/lib", relative = true }
