{
  "name": "hylopdf",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/sync-pdfjs.mjs",
    "predev": "node scripts/sync-pdfjs.mjs",
    "dev": "vite",
    "check": "tsc --noEmit",
    "build": "node scripts/sync-pdfjs.mjs && tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "test": "node tests/run.mjs"
  },
  "dependencies": {
    "pdfjs-dist": "^5.4.0"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/cli": "^2",
    "@tauri-apps/plugin-dialog": "^2",
    "playwright": "^1.62.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "esbuild": "^0.25.0"
  }
}
